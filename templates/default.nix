{ ... }:
{
  rust = {
    path = ./rust;
    description = "Rust dev shell template";
  };
  rust-bevy = {
    path = ./rust-bevy;
    description = "Rust dev shell template, used for bevy projects";
  };
}
